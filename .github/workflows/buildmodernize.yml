name: Build Modernize Browser (HTML Loader - Fixed)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify legacy-transpiler.js exists
        run: |
          if [ ! -f patches/legacy-transpiler.js ]; then
            echo "ERROR: patches/legacy-transpiler.js not found!"
            echo ""
            echo "You MUST upload your 274KB legacy-transpiler.js file to the patches/ folder"
            exit 1
          fi
          echo "legacy-transpiler.js size: $(wc -c < patches/legacy-transpiler.js) bytes"

      - name: Create app bundle
        run: |
          APP_NAME="Modernize"
          mkdir -p Payload/${APP_NAME}.app

      - name: Download runtime polyfills
        run: |
          curl -L -o patches/core-js.js https://cdn.jsdelivr.net/npm/core-js@3/actual/minified.js
          curl -L -o patches/regenerator.js https://cdn.jsdelivr.net/npm/regenerator-runtime@0.14.1/runtime.min.js
          curl -L -o patches/fetch.js https://cdn.jsdelivr.net/npm/whatwg-fetch@3.6.20/dist/fetch.umd.js
          echo "Polyfills downloaded"

      - name: Create complete HTML loader with ALL JS embedded
        run: |
          APP_NAME="Modernize"
          
          # Read all JS files into variables (escape for safe embedding)
          CORE_JS=$(cat patches/core-js.js | sed 's/\\/\\\\/g' | sed 's/`/\\`/g' | sed 's/\$/\\$/g')
          REGEN_JS=$(cat patches/regenerator.js | sed 's/\\/\\\\/g' | sed 's/`/\\`/g' | sed 's/\$/\\$/g')
          FETCH_JS=$(cat patches/fetch.js | sed 's/\\/\\\\/g' | sed 's/`/\\`/g' | sed 's/\$/\\$/g')
          LEGACY_JS=$(cat patches/legacy-transpiler.js | sed 's/\\/\\\\/g' | sed 's/`/\\`/g' | sed 's/\$/\\$/g')
          
          cat > Payload/${APP_NAME}.app/loader.html << 'HTML_EOF'
          <!DOCTYPE html>
          <html>
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
              <title>Modernize Browser</title>
              <style>
                  * { margin: 0; padding: 0; box-sizing: border-box; }
                  body { background: #000; overflow: hidden; }
                  iframe { width: 100%; height: 100%; border: none; position: fixed; top: 0; left: 0; right: 0; bottom: 0; }
              </style>
          </head>
          <body>
              <iframe id="webFrame" allow="camera; microphone; geolocation; fullscreen"></iframe>
              <script>
              // ============================================================
              // STEP 1: ES2020-2025 Basic Polyfills
              // ============================================================
              (function(global){
                  if (!global.queueMicrotask) global.queueMicrotask = function(cb){ Promise.resolve().then(cb); };
                  if (!Array.prototype.at) Array.prototype.at = function(idx){ var len=this.length,i=idx>=0?idx:len+idx; return (i<0||i>=len)?undefined:this[i]; };
                  if (!String.prototype.at) String.prototype.at = function(idx){ var len=this.length,i=idx>=0?idx:len+idx; return (i<0||i>=len)?undefined:this[i]; };
                  if (!Object.hasOwn) Object.hasOwn = function(obj,prop){ return Object.prototype.hasOwnProperty.call(obj,prop); };
                  if (!String.prototype.replaceAll) String.prototype.replaceAll = function(s,r){ if(typeof s==="string")return this.split(s).join(r); if(s instanceof RegExp){if(!s.global)throw new TypeError("Global RegExp required"); return this.replace(s,r);} return this.replace(s,r); };
                  if (typeof global.globalThis === 'undefined') global.globalThis = global;
                  if (!Promise.allSettled) {
                      Promise.allSettled = function(promises) {
                          return Promise.all(promises.map(function(p) {
                              return Promise.resolve(p).then(
                                  function(v) { return { status: 'fulfilled', value: v }; },
                                  function(r) { return { status: 'rejected', reason: r }; }
                              );
                          }));
                      };
                  }
                  if (!Promise.any) {
                      Promise.any = function(promises) {
                          return new Promise(function(resolve, reject) {
                              var errors = [];
                              var pending = promises.length;
                              if (pending === 0) reject(new AggregateError([], 'All promises rejected'));
                              for (var i = 0; i < promises.length; i++) {
                                  Promise.resolve(promises[i]).then(resolve, function(err) {
                                      errors[i] = err;
                                      if (--pending === 0) reject(new AggregateError(errors, 'All promises rejected'));
                                  });
                              }
                          });
                      };
                  }
                  if (typeof AggregateError === 'undefined') {
                      global.AggregateError = function(errors, message) {
                          var e = new Error(message);
                          e.errors = errors;
                          e.name = 'AggregateError';
                          return e;
                      };
                  }
                  if (!Array.prototype.flat) {
                      Array.prototype.flat = function(depth) {
                          var d = depth === undefined ? 1 : Math.floor(depth);
                          if (d < 1) return Array.prototype.slice.call(this);
                          return this.reduce(function(acc, val) {
                              return acc.concat(Array.isArray(val) ? val.flat(d - 1) : val);
                          }, []);
                      };
                  }
                  if (!Array.prototype.flatMap) Array.prototype.flatMap = function(cb,thisArg){ return this.map(cb,thisArg).flat(); };
                  if (!Object.fromEntries) {
                      Object.fromEntries = function(entries) {
                          var obj = {};
                          for (var i = 0; i < entries.length; i++) obj[entries[i][0]] = entries[i][1];
                          return obj;
                      };
                  }
                  if (!String.prototype.matchAll) {
                      String.prototype.matchAll = function(regexp) {
                          var flags = regexp.flags;
                          if (!flags.includes('g')) flags += 'g';
                          var rx = new RegExp(regexp.source, flags);
                          var matches = [];
                          var match;
                          while ((match = rx.exec(this)) !== null) matches.push(match);
                          return matches[Symbol.iterator]();
                      };
                  }
                  console.log('[Loader] ES2020-2025 polyfills loaded');
              })(window);
              
              // ============================================================
              // STEP 2: iOS 15 Web API Fallbacks
              // ============================================================
              (function(global){
                  if (global.MediaQueryList && !global.MediaQueryList.prototype.addEventListener) {
                      var proto = global.MediaQueryList.prototype;
                      var wrappers = new WeakMap();
                      proto.addEventListener = function(type, listener) {
                          if (type !== 'change') return;
                          if (typeof listener === 'function') proto.addListener.call(this, listener);
                          else {
                              var wrapper = wrappers.get(listener);
                              if (!wrapper) { wrapper = function(ev) { listener.handleEvent(ev); }; wrappers.set(listener, wrapper); }
                              proto.addListener.call(this, wrapper);
                          }
                      };
                      proto.removeEventListener = function(type, listener) {
                          if (type !== 'change') return;
                          if (typeof listener === 'function') proto.removeListener.call(this, listener);
                          else {
                              var wrapper = wrappers.get(listener);
                              if (wrapper) proto.removeListener.call(this, wrapper);
                          }
                      };
                  }
                  if (!global.CSS) global.CSS = {};
                  if (typeof CSS.supports !== 'function') {
                      CSS.supports = function(prop, value) {
                          var el = document.createElement('div');
                          if (arguments.length === 1) {
                              var pair = prop.split(':');
                              return typeof el.style[pair[0].trim()] !== 'undefined';
                          }
                          return typeof el.style[prop] !== 'undefined';
                      };
                  }
                  if (typeof requestIdleCallback !== 'function') {
                      global.requestIdleCallback = function(cb) {
                          return setTimeout(function() { cb({ didTimeout: false, timeRemaining: function() { return 50; } }); }, 1);
                      };
                      global.cancelIdleCallback = function(id) { clearTimeout(id); };
                  }
                  if (typeof CustomEvent !== 'function') {
                      global.CustomEvent = function(type, options) {
                          var ev = document.createEvent('CustomEvent');
                          ev.initCustomEvent(type, !!(options && options.bubbles), !!(options && options.cancelable), options && options.detail);
                          return ev;
                      };
                  }
                  if (!Element.prototype.scrollTo) {
                      Element.prototype.scrollTo = function(x, y) {
                          if (typeof x === 'object') { this.scrollLeft = x.left || 0; this.scrollTop = x.top || 0; }
                          else { this.scrollLeft = x || 0; this.scrollTop = y || 0; }
                      };
                  }
                  console.log('[Loader] iOS 15 fallbacks loaded');
              })(window);
              
              // ============================================================
              // STEP 3: core-js (embedded)
              // ============================================================
              try {
                  ${CORE_JS}
                  console.log('[Loader] core-js loaded');
              } catch(e) { console.error('[Loader] core-js error:', e); }
              
              // ============================================================
              // STEP 4: regenerator-runtime (embedded)
              // ============================================================
              try {
                  ${REGEN_JS}
                  console.log('[Loader] regenerator loaded');
              } catch(e) { console.error('[Loader] regenerator error:', e); }
              
              // ============================================================
              // STEP 5: whatwg-fetch (embedded)
              // ============================================================
              try {
                  ${FETCH_JS}
                  console.log('[Loader] fetch loaded');
              } catch(e) { console.error('[Loader] fetch error:', e); }
              
              // ============================================================
              // STEP 6: YOUR LegacyTranspiler (embedded)
              // ============================================================
              try {
                  ${LEGACY_JS}
                  console.log('[Loader] LegacyTranspiler loaded');
              } catch(e) { console.error('[Loader] LegacyTranspiler error:', e); }
              
              // ============================================================
              // STEP 7: Initialize and load website
              // ============================================================
              var iframe = document.getElementById('webFrame');
              var targetURL = "https://z.ai";
              var userAgent = "Mozilla/5.0 (iPhone; CPU iPhone OS 26_0 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/26.0 Mobile/15E148 Safari/604.1";
              
              function initTranspiler() {
                  if (typeof window.LegacyTranspiler !== 'undefined' && window.LegacyTranspiler.init) {
                      window.LegacyTranspiler.init({
                          BASE_URL: targetURL,
                          runScript: function(code) {
                              try {
                                  var script = document.createElement("script");
                                  script.textContent = code;
                                  document.head.appendChild(script);
                                  script.remove();
                              } catch(e) { console.error(e); }
                          },
                          target: { platform: "iOS", version: "15.0" },
                          minify: true
                      });
                      console.log('[Loader] LegacyTranspiler initialized');
                  } else {
                      console.warn('[Loader] LegacyTranspiler not found');
                  }
              }
              
              function loadWebsite() {
                  initTranspiler();
                  iframe.src = targetURL;
                  console.log('[Loader] Loading: ' + targetURL);
              }
              
              if (document.readyState === 'loading') {
                  document.addEventListener('DOMContentLoaded', loadWebsite);
              } else {
                  loadWebsite();
              }
              
              // Handle iframe navigation updates (for URL bar)
              iframe.addEventListener('load', function() {
                  try {
                      var iframeURL = iframe.contentWindow.location.href;
                      if (iframeURL && iframeURL !== targetURL) {
                          // Send message to Swift (will be handled by WebKit message handler)
                          window.webkit?.messageHandlers?.urlUpdate?.postMessage(iframeURL);
                      }
                  } catch(e) { /* cross-origin, ignore */ }
              });
              </script>
          </body>
          </html>
          HTML_EOF
          
          # Replace the placeholders with actual JS content
          sed -i '' "s|\${CORE_JS}|$(cat patches/core-js.js | sed 's/\\/\\\\/g' | sed 's/&/\\&/g' | sed 's/\//\\\//g' | tr -d '\n')|" Payload/${APP_NAME}.app/loader.html
          sed -i '' "s|\${REGEN_JS}|$(cat patches/regenerator.js | sed 's/\\/\\\\/g' | sed 's/&/\\&/g' | sed 's/\//\\\//g' | tr -d '\n')|" Payload/${APP_NAME}.app/loader.html
          sed -i '' "s|\${FETCH_JS}|$(cat patches/fetch.js | sed 's/\\/\\\\/g' | sed 's/&/\\&/g' | sed 's/\//\\\//g' | tr -d '\n')|" Payload/${APP_NAME}.app/loader.html
          sed -i '' "s|\${LEGACY_JS}|$(cat patches/legacy-transpiler.js | sed 's/\\/\\\\/g' | sed 's/&/\\&/g' | sed 's/\//\\\//g' | tr -d '\n')|" Payload/${APP_NAME}.app/loader.html
          
          echo "HTML loader created with embedded JS"
          ls -la Payload/${APP_NAME}.app/

      - name: Generate Swift source
        run: |
          cat > app.swift << 'SWIFT'
          import UIKit
          import WebKit

          class ViewController: UIViewController, WKNavigationDelegate, WKUIDelegate, WKScriptMessageHandler, UITextFieldDelegate, UIGestureRecognizerDelegate {
              
              var webView: WKWebView!
              var urlTextField: UITextField!
              var backButton: UIButton!
              var forwardButton: UIButton!
              var reloadButton: UIButton!
              var progressView: UIProgressView!
              var bottomToolbar: UIView!
              var bottomConstraint: NSLayoutConstraint!
              private var toolbarHidden = false
              private var currentURL: URL?
              
              override func viewDidLoad() {
                  super.viewDidLoad()
                  view.backgroundColor = UIColor.systemBackground
                  
                  setupWebView()
                  setupUI()
                  setupKeyboardHandling()
                  setupSwipeGestures()
                  
                  // Load local loader HTML
                  if let loaderPath = Bundle.main.path(forResource: "loader", ofType: "html"),
                     let url = URL(string: "file://" + loaderPath) {
                      webView.loadFileURL(url, allowingReadAccessTo: url.deletingLastPathComponent())
                      currentURL = url
                  }
              }
              
              func setupWebView() {
                  let config = WKWebViewConfiguration()
                  config.allowsInlineMediaPlayback = true
                  config.mediaTypesRequiringUserActionForPlayback = []
                  config.userContentController.add(self, name: "urlUpdate")
                  
                  webView = WKWebView(frame: .zero, configuration: config)
                  webView.translatesAutoresizingMaskIntoConstraints = false
                  webView.navigationDelegate = self
                  webView.uiDelegate = self
                  webView.allowsBackForwardNavigationGestures = true
                  view.addSubview(webView)
                  webView.addObserver(self, forKeyPath: #keyPath(WKWebView.estimatedProgress), options: .new, context: nil)
              }
              
              func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
                  if message.name == "urlUpdate", let urlString = message.body as? String {
                      DispatchQueue.main.async {
                          self.urlTextField.text = urlString
                      }
                  }
              }
              
              func setupUI() {
                  progressView = UIProgressView(progressViewStyle: .bar)
                  progressView.translatesAutoresizingMaskIntoConstraints = false
                  progressView.progressTintColor = UIColor.systemBlue
                  view.addSubview(progressView)
                  
                  bottomToolbar = UIView()
                  bottomToolbar.translatesAutoresizingMaskIntoConstraints = false
                  bottomToolbar.backgroundColor = UIColor.systemBackground
                  bottomToolbar.layer.shadowColor = UIColor.black.cgColor
                  bottomToolbar.layer.shadowOffset = CGSize(width: 0, height: -2)
                  bottomToolbar.layer.shadowOpacity = 0.1
                  view.addSubview(bottomToolbar)
                  
                  urlTextField = UITextField()
                  urlTextField.translatesAutoresizingMaskIntoConstraints = false
                  urlTextField.placeholder = "Search or enter URL"
                  urlTextField.borderStyle = .roundedRect
                  urlTextField.backgroundColor = UIColor.systemGray6
                  urlTextField.autocapitalizationType = .none
                  urlTextField.autocorrectionType = .no
                  urlTextField.keyboardType = .URL
                  urlTextField.returnKeyType = .go
                  urlTextField.delegate = self
                  urlTextField.clearButtonMode = .whileEditing
                  bottomToolbar.addSubview(urlTextField)
                  
                  let buttonStack = UIStackView()
                  buttonStack.translatesAutoresizingMaskIntoConstraints = false
                  buttonStack.axis = .horizontal
                  buttonStack.distribution = .equalSpacing
                  buttonStack.alignment = .center
                  buttonStack.spacing = 20
                  bottomToolbar.addSubview(buttonStack)
                  
                  func btn(_ name: String, _ action: Selector) -> UIButton {
                      let b = UIButton(type: .system)
                      b.setImage(UIImage(systemName: name), for: .normal)
                      b.addTarget(self, action: action, for: .touchUpInside)
                      b.tintColor = UIColor.label
                      return b
                  }
                  
                  backButton = btn("chevron.left", #selector(goBack))
                  forwardButton = btn("chevron.right", #selector(goForward))
                  reloadButton = btn("arrow.clockwise", #selector(reload))
                  
                  buttonStack.addArrangedSubview(backButton)
                  buttonStack.addArrangedSubview(forwardButton)
                  buttonStack.addArrangedSubview(reloadButton)
                  
                  NSLayoutConstraint.activate([
                      webView.topAnchor.constraint(equalTo: view.topAnchor),
                      webView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
                      webView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
                      webView.bottomAnchor.constraint(equalTo: bottomToolbar.topAnchor),
                      
                      progressView.topAnchor.constraint(equalTo: webView.bottomAnchor),
                      progressView.leadingAnchor.constraint(equalTo: view.leadingAnchor),
                      progressView.trailingAnchor.constraint(equalTo: view.trailingAnchor),
                      progressView.heightAnchor.constraint(equalToConstant: 2),
                      
                      bottomToolbar.leadingAnchor.constraint(equalTo: view.leadingAnchor),
                      bottomToolbar.trailingAnchor.constraint(equalTo: view.trailingAnchor),
                      bottomToolbar.heightAnchor.constraint(equalToConstant: 100),
                      
                      buttonStack.leadingAnchor.constraint(equalTo: bottomToolbar.leadingAnchor, constant: 20),
                      buttonStack.trailingAnchor.constraint(equalTo: bottomToolbar.trailingAnchor, constant: -20),
                      buttonStack.topAnchor.constraint(equalTo: bottomToolbar.topAnchor, constant: 8),
                      buttonStack.heightAnchor.constraint(equalToConstant: 44),
                      
                      urlTextField.topAnchor.constraint(equalTo: buttonStack.bottomAnchor, constant: 4),
                      urlTextField.leadingAnchor.constraint(equalTo: bottomToolbar.leadingAnchor, constant: 16),
                      urlTextField.trailingAnchor.constraint(equalTo: bottomToolbar.trailingAnchor, constant: -16),
                      urlTextField.heightAnchor.constraint(equalToConstant: 40),
                      urlTextField.bottomAnchor.constraint(equalTo: bottomToolbar.bottomAnchor, constant: -8)
                  ])
                  
                  bottomConstraint = bottomToolbar.bottomAnchor.constraint(equalTo: view.safeAreaLayoutGuide.bottomAnchor)
                  bottomConstraint.isActive = true
              }
              
              func setupSwipeGestures() {
                  let swipeUp = UISwipeGestureRecognizer(target: self, action: #selector(handleSwipeUp))
                  swipeUp.direction = .up
                  swipeUp.delegate = self
                  webView.addGestureRecognizer(swipeUp)
                  
                  let swipeDown = UISwipeGestureRecognizer(target: self, action: #selector(handleSwipeDown))
                  swipeDown.direction = .down
                  swipeDown.delegate = self
                  webView.addGestureRecognizer(swipeDown)
              }
              
              @objc func handleSwipeUp() {
                  guard !toolbarHidden else { return }
                  toolbarHidden = true
                  UIView.animate(withDuration: 0.3) {
                      self.bottomConstraint.constant = -self.bottomToolbar.bounds.height
                      self.view.layoutIfNeeded()
                  }
              }
              
              @objc func handleSwipeDown() {
                  guard toolbarHidden else { return }
                  toolbarHidden = false
                  UIView.animate(withDuration: 0.3) {
                      self.bottomConstraint.constant = 0
                      self.view.layoutIfNeeded()
                  }
              }
              
              func gestureRecognizer(_ gestureRecognizer: UIGestureRecognizer, shouldRecognizeSimultaneouslyWith otherGestureRecognizer: UIGestureRecognizer) -> Bool {
                  return true
              }
              
              func setupKeyboardHandling() {
                  NotificationCenter.default.addObserver(self, selector: #selector(keyboardWillShow), name: UIResponder.keyboardWillShowNotification, object: nil)
                  NotificationCenter.default.addObserver(self, selector: #selector(keyboardWillHide), name: UIResponder.keyboardWillHideNotification, object: nil)
                  let tap = UITapGestureRecognizer(target: self, action: #selector(dismissKeyboard))
                  tap.cancelsTouchesInView = false
                  webView.addGestureRecognizer(tap)
              }
              
              @objc func keyboardWillShow(_ notification: Notification) {
                  guard let keyboardFrame = notification.userInfo?[UIResponder.keyboardFrameEndUserInfoKey] as? CGRect else { return }
                  let duration = notification.userInfo?[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
                  let curve = notification.userInfo?[UIResponder.keyboardAnimationCurveUserInfoKey] as? UInt ?? 7
                  UIView.animate(withDuration: duration, delay: 0, options: UIView.AnimationOptions(rawValue: curve << 16)) {
                      self.bottomConstraint.constant = -keyboardFrame.height + self.view.safeAreaInsets.bottom
                      self.view.layoutIfNeeded()
                  }
              }
              
              @objc func keyboardWillHide(_ notification: Notification) {
                  let duration = notification.userInfo?[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
                  let curve = notification.userInfo?[UIResponder.keyboardAnimationCurveUserInfoKey] as? UInt ?? 7
                  UIView.animate(withDuration: duration, delay: 0, options: UIView.AnimationOptions(rawValue: curve << 16)) {
                      self.bottomConstraint.constant = self.toolbarHidden ? -self.bottomToolbar.bounds.height : 0
                      self.view.layoutIfNeeded()
                  }
              }
              
              @objc func dismissKeyboard() { urlTextField.resignFirstResponder() }
              
              func loadURL(_ url: URL) {
                  let js = "var iframe = document.getElementById('webFrame'); iframe.src = '\(url.absoluteString)';"
                  webView.evaluateJavaScript(js) { _, error in
                      if let error = error { print("JS error: \(error)") }
                  }
                  urlTextField.text = url.absoluteString
              }
              
              func loadSearch(_ query: String) {
                  let encoded = query.addingPercentEncoding(withAllowedCharacters: .urlQueryAllowed) ?? query
                  if let url = URL(string: "https://www.google.com/search?q=\(encoded)") {
                      loadURL(url)
                  }
              }
              
              @objc func goBack() { webView.goBack() }
              @objc func goForward() { webView.goForward() }
              @objc func reload() {
                  if webView.isLoading { webView.stopLoading() }
                  else { webView.reload() }
              }
              
              func textFieldShouldReturn(_ textField: UITextField) -> Bool {
                  textField.resignFirstResponder()
                  guard let text = textField.text, !text.isEmpty else { return true }
                  if text.hasPrefix("http://") || text.hasPrefix("https://") {
                      if let url = URL(string: text) { loadURL(url) }
                  } else if text.contains(".") && !text.contains(" ") {
                      if let url = URL(string: "https://\(text)") { loadURL(url) }
                  } else {
                      loadSearch(text)
                  }
                  return true
              }
              
              override func observeValue(forKeyPath keyPath: String?, of object: Any?, change: [NSKeyValueChangeKey : Any]?, context: UnsafeMutableRawPointer?) {
                  if keyPath == #keyPath(WKWebView.estimatedProgress) {
                      progressView.progress = Float(webView.estimatedProgress)
                      progressView.isHidden = webView.estimatedProgress >= 1.0
                  }
              }
              
              func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
                  urlTextField.text = webView.url?.absoluteString
                  backButton.isEnabled = webView.canGoBack
                  forwardButton.isEnabled = webView.canGoForward
              }
              
              func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
                  print("Navigation failed: \(error.localizedDescription)")
              }
          }

          class AppDelegate: UIResponder, UIApplicationDelegate {
              var window: UIWindow?
              func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                  window = UIWindow(frame: UIScreen.main.bounds)
                  window?.rootViewController = ViewController()
                  window?.makeKeyAndVisible()
                  return true
              }
          }

          UIApplicationMain(CommandLine.argc, CommandLine.unsafeArgv, nil, NSStringFromClass(AppDelegate.self))
          SWIFT

      - name: Compile Swift
        run: |
          APP_NAME="Modernize"
          SDK=$(xcrun --sdk iphoneos --show-sdk-path)
          xcrun -sdk iphoneos swiftc -target arm64-apple-ios15.0 -framework UIKit -framework WebKit -o Payload/${APP_NAME}.app/${APP_NAME} app.swift
          chmod +x Payload/${APP_NAME}.app/${APP_NAME}

      - name: Create Info.plist
        run: |
          APP_NAME="Modernize"
          cat > Payload/${APP_NAME}.app/Info.plist << 'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
          <key>CFBundleExecutable</key><string>Modernize</string>
          <key>CFBundleIdentifier</key><string>com.modernize.browser</string>
          <key>CFBundleName</key><string>Modernize</string>
          <key>CFBundleDisplayName</key><string>Modernize</string>
          <key>CFBundlePackageType</key><string>APPL</string>
          <key>CFBundleShortVersionString</key><string>1.0</string>
          <key>CFBundleVersion</key><string>1</string>
          <key>LSRequiresIPhoneOS</key><true/>
          <key>MinimumOSVersion</key><string>15.0</string>
          <key>UIDeviceFamily</key><array><integer>1</integer><integer>2</integer></array>
          <key>UISupportedInterfaceOrientations</key><array><string>UIInterfaceOrientationPortrait</string><string>UIInterfaceOrientationLandscapeLeft</string><string>UIInterfaceOrientationLandscapeRight</string></array>
          <key>UIViewControllerBasedStatusBarAppearance</key><true/>
          <key>NSAppTransportSecurity</key><dict><key>NSAllowsArbitraryLoads</key><true/></dict>
          <key>NSMicrophoneUsageDescription</key><string>Modernize needs microphone</string>
          <key>NSCameraUsageDescription</key><string>Modernize needs camera</string>
          <key>NSPhotoLibraryUsageDescription</key><string>Modernize needs photo library</string>
          <key>UIFileSharingEnabled</key><true/>
          <key>CFBundleURLTypes</key><array><dict><key>CFBundleURLSchemes</key><array><string>modernize</string><string>http</string><string>https</string></array></dict></array>
          <key>CFBundleIcons</key><dict><key>CFBundlePrimaryIcon</key><dict><key>CFBundleIconFiles</key><array><string>Icon-60</string><string>Icon-76</string></array></dict></dict>
          </dict></plist>
          PLIST

      - name: Create PkgInfo
        run: |
          APP_NAME="Modernize"
          printf 'APPL????' > Payload/${APP_NAME}.app/PkgInfo

      - name: Create IPA
        run: |
          APP_NAME="Modernize"
          zip -r ${APP_NAME}.ipa Payload/

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Modernize-HTML-Loader-Fixed
          path: Modernize.ipa
